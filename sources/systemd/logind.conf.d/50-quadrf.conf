# Pi 5 pwr_button is gpio-keys KEY_POWER (evdev). DietPi masks logind, which
# is the handler; acpid never sees this device. Ignore inhibitors so a
# KasmVNC or PulseAudio lock cannot block the physical button.
[Login]
HandlePowerKey=poweroff
PowerKeyIgnoreInhibited=yes
