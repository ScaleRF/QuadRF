# DietPi dietpi-ramlog mounts tmpfs on /var/log, so the directory from the
# nginx package is gone by the time ExecStartPre runs nginx -t.
[Service]
ExecStartPre=
ExecStartPre=/bin/mkdir -p /var/log/nginx
ExecStartPre=/usr/sbin/nginx -t -q -g 'daemon on; master_process on;'
