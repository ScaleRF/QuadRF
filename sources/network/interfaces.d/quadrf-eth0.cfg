# Ethernet: DHCP on a router, or 10.55.1.1 direct-connect if no lease.
# quadrf-ethernet decides which; no gateway in direct-connect mode.
allow-hotplug eth0
iface eth0 inet manual
    up /usr/sbin/quadrf-ethernet
    down /usr/sbin/quadrf-ethernet stop
