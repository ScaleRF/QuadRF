# Wi-Fi. quadrf-hotspot owns client vs AP; do not dhcp here or ifup
# will lease from our own access point and overwrite resolv.conf.
allow-hotplug wlan0
iface wlan0 inet manual
    pre-up iw dev wlan0 set power_save off || true
    post-down iw dev wlan0 set power_save on || true
